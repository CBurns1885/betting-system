# ============================================================================
# FOOTBALL PREDICTION SYSTEM - REQUIREMENTS
# Full version with all dependencies for maximum performance
# ============================================================================

# Core ML/Data Science Stack (REQUIRED)
numpy>=1.21.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.7.0
joblib>=1.0.0

# Gradient Boosting Libraries (REQUIRED for full performance)
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0

# Hyperparameter Tuning (REQUIRED - used by training_settings.json)
optuna>=3.0.0

# PyTorch for Neural Networks (OPTIONAL - used for BNN models)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Data Processing & I/O (REQUIRED)
requests>=2.28.0
openpyxl>=3.1.0
beautifulsoup4>=4.11.0

# Visualization (OPTIONAL - for analysis reports)
matplotlib>=3.5.0
seaborn>=0.12.0

# Additional utilities (standard library, no install needed)
# - sqlite3 (built-in)
# - smtplib (built-in)
# - json (built-in)
# - datetime (built-in)
# - pathlib (built-in)
# - pickle (built-in)
# - collections (built-in)
# - typing (built-in)
# - warnings (built-in)